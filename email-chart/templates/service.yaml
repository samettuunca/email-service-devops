apiVersion: v1
kind: Service
metadata:
  name: {{ include "email-chart.fullname" . }}
  labels:
    {{- include "email-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "email-chart.selectorLabels" . | nindent 4 }}
